给你一个整数数组 nums ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。

子数组
是数组中的一个连续部分。

 

示例 1：

输入：nums = [-2,1,-3,4,-1,2,1,-5,4]
输出：6
解释：连续子数组 [4,-1,2,1] 的和最大，为 6 。
示例 2：

输入：nums = [1]
输出：1
示例 3：

输入：nums = [5,4,-1,7,8]
输出：23
 

提示：

1 <= nums.length <= 105
-104 <= nums[i] <= 104



解题思路：
遍历nums，对于每一位元素，计算以该元素为子数组结尾时，从下标为0的元素到该元素的最大子数组和。
同时，随着遍历，迭代最大的和，最后返回时输出结果。
时间复杂度o(n), 空间复杂度o(1)
